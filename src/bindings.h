
#ifndef __GOGL_BINDINGS_H__
#define __GOGL_BINDINGS_H__

#include <Crpc.h>

void bindMethods(point_t *p);

void bindOpenglMethods(point_t *p);
void bindGlfwMethods(point_t *p);

#endif // __GOGL_BINDINGS_H__
